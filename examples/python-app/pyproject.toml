[project]
name = "trussium-example-app"
version = "0.1.0"
description = "Example FastAPI application using an existing Trussium runtime."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.116.0",
    "trussium-sdk>=0.1.0",
    "uvicorn[standard]>=0.35.0",
]
